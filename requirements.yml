name: project_21
channels:
  - defaults
dependencies:
  - python=3.8  # Adjust the Python version if needed
  - dlib
  - scikit-learn
  - opencv
  - matplotlib
  - numpy
  - tensorflow
  - tk  # For tkinter
  - pillow  # For PIL (Pillow is the modern version of PIL)
  # Add any other dependencies as needed
  # Generated by ChatGPT
